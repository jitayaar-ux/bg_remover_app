name: jeeta_sms_app
description: Jeeta SMS App - Telegram style secure SMS app with profile, OTP, and in-app chat.
publish_to: 'none'

environment:
  sdk: '^3.13.0'

dependencies:
  flutter:
    sdk: flutter
  url_launcher: ^6.2.5
  permission_handler: ^11.3.0
  intl: ^0.19.0

flutter:
  uses-material-design: true
  
