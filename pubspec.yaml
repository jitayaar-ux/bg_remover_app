name: jeeta_sms_app
description: Jeeta SMS App with secure password login, profile, and dual SIM chat.
publish_to: 'none'

environment:
  sdk: '^3.13.0'

dependencies:
  flutter:
    sdk: flutter
  url_launcher: ^6.2.5
  permission_handler: ^11.3.0
  shared_preferences: ^2.2.2
  intl: ^0.19.0

flutter:
  uses-material-design: true
  
