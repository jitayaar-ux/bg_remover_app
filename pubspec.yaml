name: bg_remover_app
description: Pro photo editor with AI auto remove, HD clear, crop, and eraser.
publish_to: 'none'

environment:
  sdk: '^3.13.0'

dependencies:
  flutter:
    sdk: flutter
  image_picker: ^1.0.7
  image: ^4.1.3
  path_provider: ^2.1.2
  gal: ^2.2.0
  crop_image: ^1.0.17

flutter:
  uses-material-design: true
  
